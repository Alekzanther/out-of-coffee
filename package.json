{
  "name": "root",
  "private": true,
  "packageManager": "pnpm@6.32.2",
  "scripts": {
    "start": "pnpm start --prefix packages/client/",
    "serve": "pnpm start --prefix packages/server/",
    "dev": "pnpm recursive run dev --filter ./packages/server --filter ./packages/client",
    "dev:client": "pnpm recursive run dev --filter ./packages/client",
    "dev:server": "pnpm recursive run dev --filter ./packages/server"
  }
}
