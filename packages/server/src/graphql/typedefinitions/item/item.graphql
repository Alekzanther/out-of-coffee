type Item @entity {
  _id: String! @id
  name: String! @column
  productUrl: String! @column
  productImageUrl: String @column
}

input NewItem {
  name: String!
  productUrl: String!
}

type Mutation {
  CreateItem(newItem: NewItem!): Item
}

type Query {
  GetItem(id: ID!): Item
  GetItems: [Item]
}
