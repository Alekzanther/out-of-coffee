overwrite: true
schema: 'lib/apollo-server/typedefinitions/**/*.graphql'
generates:
  apollo-generated/server-graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers
      - typescript-mongodb
