type BaseOrder @entity {
  _id: String! @id
  items: [Item] @link
}

input NewBaseOrder {
  items: [String!]!
}

type Query {
  getBaseOrder: BaseOrder!
}

type Mutation {
  setBaseOrder(newBaseOrder: NewBaseOrder!): BaseOrder!
}
